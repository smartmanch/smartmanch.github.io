// Global Variables for Quiz
const SCHOOL_NAME = "राजकीय उच्च माध्यमिक विद्यालय सैनीपुरा, झुंझुनू";

// --- START: Full Pool of 30 Objective Questions for 'Chemical Reactions and Equations' ---

const objectiveQuestions = [
    { word: "मैग्नीशियम रिबन को हवा में जलाने पर किस नए पदार्थ का निर्माण होता है?", options: ["मैग्नीशियम क्लोराइड", "मैग्नीशियम ऑक्साइड", "कार्बन डाइऑक्साइड", "जल"], answer: 1 },
    { word: "वह अभिक्रिया जिसमें ऊष्मा उत्सर्जित होती है, क्या कहलाती है?", options: ["ऊष्माशोषी", "ऊष्माक्षेपी", "वियोजन", "विस्थापन"], answer: 1 },
    { word: "बुझे हुए चूने का रासायनिक सूत्र क्या है?", options: ["CaCO3", "CaO", "Ca(OH)2", "HCl"], answer: 2 },
    { word: "किसी रासायनिक अभिक्रिया में अभिकारकों और उत्पादों के परमाणुओं की संख्या को बराबर करना क्या कहलाता है?", options: ["असंतुलन", "मोल संकल्पना", "संयोजन", "समीकरण को संतुलित करना"], answer: 3 },
    { word: "श्वसन किस प्रकार की रासायनिक अभिक्रिया है?", options: ["संयोजन", "ऊष्माशोषी", "विस्थापन", "ऊष्माक्षेपी"], answer: 3 },
    { word: "जब दो या दो से अधिक अभिकारक मिलकर एक एकल उत्पाद बनाते हैं, तो वह कौन सी अभिक्रिया कहलाती है?", options: ["संयोजन", "वियोजन", "विस्थापन", "द्वि-विस्थापन"], answer: 0 },
    { word: "सूर्य के प्रकाश की उपस्थिति में सिल्वर क्लोराइड का सिल्वर और क्लोरीन में टूटना किस प्रकार की अभिक्रिया है?", options: ["संयोजन", "प्रकाश-वियोजन", "ऊष्माशोषी", "उपचयन"], answer: 1 },
    { word: "लोहे की कील को कॉपर सल्फेट के विलयन में डुबोने पर विलयन का रंग क्यों बदल जाता है?", options: ["संयोजन", "द्वि-विस्थापन", "वियोजन", "विस्थापन"], answer: 3 },
    { word: "अपचयन (Reduction) अभिक्रिया में क्या होता है?", options: ["ऑक्सीजन की वृद्धि", "हाइड्रोजन का हास", "हाइड्रोजन की वृद्धि", "तापमान में वृद्धि"], answer: 2 },
    { word: "संक्षारण (Corrosion) का उदाहरण है:", options: ["भोजन का पाचन", "मैग्नीशियम का जलना", "लोहे पर जंग लगना", "पानी का उबलना"], answer: 2 },
    { word: "विकृतगंधिता (Rancidity) से बचने के लिए चिप्स के पैकेट में कौन सी गैस भरी जाती है?", options: ["ऑक्सीजन", "कार्बन डाइऑक्साइड", "नाइट्रोजन", "हाइड्रोजन"], answer: 2 },
    { word: "लेड नाइट्रेट और पोटेशियम आयोडाइड के बीच अभिक्रिया किस प्रकार की है?", options: ["वियोजन", "विस्थापन", "संयोजन", "द्वि-विस्थापन"], answer: 3 },
    { word: "अभिकारक किसे कहते हैं?", options: ["वे पदार्थ जो अभिक्रिया के बाद बनते हैं", "वे पदार्थ जो अभिक्रिया में हिस्सा लेते हैं", "अभिक्रिया को प्रेरित करने वाला बल", "उत्पाद का अंतिम रूप"], answer: 1 },
    { word: "रासायनिक अभिक्रिया के दौरान गैस का उत्सर्जन होना किसका संकेत है?", options: ["भौतिक परिवर्तन", "रासायनिक परिवर्तन", "संतुलन", "कोई परिवर्तन नहीं"], answer: 1 },
    { word: "निम्नलिखित में से कौन ऊष्माशोषी अभिक्रिया का उदाहरण है?", options: ["प्राकृतिक गैस का दहन", "कैल्शियम ऑक्साइड का जल से मिलना", "बर्फ का पिघलना", "कैल्शियम कार्बोनेट का टूटना"], answer: 3 },
    { word: "एक रासायनिक समीकरण में तत्वों के प्रतीकों के साथ उनकी भौतिक अवस्था को दर्शाने की विधि क्या है?", options: ["शब्द समीकरण", "संतुलन", "रासायनिक सूत्र", "संपूर्ण समीकरण"], answer: 3 },
    { word: "द्वि-विस्थापन अभिक्रिया में अभिकारकों के बीच क्या होता है?", options: ["परमाणुओं का निर्माण", "द्रव्यमान का विनाश", "आयनों का आदान-प्रदान", "रंग में कोई बदलाव नहीं"], answer: 2 },
    { word: "किसी पदार्थ में ऑक्सीजन की वृद्धि होना कहलाता है:", options: ["अपचयन", "संक्षारण", "उपचयन", "वियोजन"], answer: 2 },
    { word: "लोहे की वस्तुओं को जंग लगने से बचाने के लिए क्या किया जाता है?", options: ["पानी में भिगोना", "पेंट या ग्रीस लगाना", "नमक मिलाना", "ऑक्सीजन की आपूर्ति बढ़ाना"], answer: 1 },
    { word: "जब कार्बन को ऑक्सीजन में जलाया जाता है, तो कौन सा उत्पाद बनता है?", options: ["कार्बन मोनोऑक्साइड", "नाइट्रोजन डाइऑक्साइड", "कार्बन डाइऑक्साइड", "जल"], answer: 2 },
];

// 10 Short/Long Answer Questions (for reading material) - CLEANED CONTENT
const shortAnswerQuestions = [
    { q: "रासायनिक अभिक्रिया क्या है? एक उदाहरण दें।", a: "ऐसे परिवर्तन जिसमें नए गुणों वाले पदार्थों का निर्माण होता है, उसे रासायनिक अभिक्रिया कहते हैं। उदाहरण: मैग्नीशियम रिबन का जलना, भोजन का पाचन, लोहे पर जंग लगना।" },
    { q: "रासायनिक समीकरण को संतुलित करना क्यों आवश्यक है?", a: "रासायनिक समीकरण को संतुलित करना **द्रव्यमान संरक्षण के नियम (Law of Conservation of Mass)** का पालन सुनिश्चित करने के लिए आवश्यक है। नियम के अनुसार, किसी भी रासायनिक अभिक्रिया में द्रव्यमान का न तो निर्माण होता है और न ही विनाश। इसलिए, अभिक्रिया से पहले (अभिकारक) और बाद में (उत्पाद) प्रत्येक तत्व के परमाणुओं की संख्या **बराबर** रहनी चाहिए।" },
    { q: "संयोजन और वियोजन अभिक्रियाओं में अंतर स्पष्ट करें। प्रत्येक का एक उदाहरण दें।", a: "संयोजन: दो या दो से अधिक अभिकारक मिलकर **एकल उत्पाद** बनाते हैं। उदाहरण: C + O₂ → CO₂ (कार्बन और ऑक्सीजन मिलकर कार्बन डाइऑक्साइड बनाते हैं)। वियोजन: एक अभिकारक टूटकर दो या दो से अधिक उत्पाद बनाता है। उदाहरण: 2FeSO₄ → Fe₂O₃ + SO₂ + SO₃ (फेरस सल्फेट का टूटना)।" },
    { q: "ऊष्माक्षेपी (Exothermic) और ऊष्माशोषी (Endothermic) अभिक्रियाओं में क्या अंतर है?", a: "ऊष्माक्षेपी: वह अभिक्रिया जिसमें उत्पाद के निर्माण के साथ **ऊष्मा उत्सर्जित** होती है। (उदाहरण: प्राकृतिक गैस का दहन)। ऊष्माशोषी: वह अभिक्रिया जिसमें ऊष्मा **अवशोषित** होती है। (उदाहरण: कैल्शियम कार्बोनेट का वियोजन या जल में ग्लूकोज घोलना)।" },
    { q: "विस्थापन और द्वि-विस्थापन अभिक्रियाओं में अंतर स्पष्ट करें।", a: "विस्थापन: इस अभिक्रिया में अधिक क्रियाशील तत्व कम क्रियाशील तत्व को उसके यौगिक से विस्थापित कर देता है। (उदाहरण: Fe + CuSO₄ → FeSO₄ + Cu)। द्वि-विस्थापन: इस अभिक्रिया में अभिकारकों के बीच **आयनों का आदान-प्रदान** होता है। (उदाहरण: Na₂SO₄ + BaCl₂ → BaSO₄ + 2NaCl)।" },
    { q: "उपचयन (Oxidation) और अपचयन (Reduction) को परिभाषित करें।", a: "उपचयन: जब किसी पदार्थ में **ऑक्सीजन की वृद्धि** या **हाइड्रोजन का हास** होता है। अपचयन: जब किसी पदार्थ में **ऑक्सीजन का हास** या **हाइड्रोजन की वृद्धि** होती है।" },
    { q: "रेडॉक्स (Redox) अभिक्रिया किसे कहते हैं? उदाहरण दें।", a: "ऐसी अभिक्रिया जिसमें एक अभिकारक **उपचयित** होता है और दूसरा **अपचयित** होता है, रेडॉक्स अभिक्रिया कहलाती है। उदाहरण: CuO + H₂ → Cu + H₂O। (यहाँ कॉपर ऑक्साइड अपचयित हो रहा है और हाइड्रोजन उपचयित हो रहा है।)" },
    { q: "संक्षारण (Corrosion) क्या है? इससे बचाव के दो तरीके लिखिए।", a: "संक्षारण: जब कोई धातु, ऑक्सीजन, आर्द्रता (नमी), अम्ल आदि के संपर्क में आती है, जिससे धातु की ऊपरी परत धीरे-धीरे कमजोर और नष्ट हो जाती है (जैसे लोहे पर जंग लगना)। बचाव के तरीके: 1. पेंट या ग्रीस लगाना। 2. यशद लेपन (Galvanization) करना।" },
    { q: "विकृतगंधिता (Rancidity) क्या है? इससे बचने के लिए क्या उपाय किए जाते हैं?", a: "विकृतगंधिता: वसायुक्त या तैलीय खाद्य पदार्थों के उपचयित (Oxidised) होने पर उनके स्वाद और गंध में परिवर्तन आ जाना। उपाय: 1. वायुरोधी बर्तनों में रखना। 2. पैकेटों में नाइट्रोजन जैसी अक्रिय गैस भरना।" },
    { q: "रासायनिक अभिक्रिया के दौरान होने वाले चार मुख्य प्रेक्षण (Observations) क्या हैं?", a: "1. अवस्था में परिवर्तन (Change in State)। 2. रंग में परिवर्तन (Change in Color)। 3. तापमान में परिवर्तन (Change in Temperature)। 4. गैस का उत्सर्जन (Evolution of Gas)।" }
];


let currentQuestionIndex = 0;
let totalCoins = 0;
let studentName = "";
let studentRoll = "";
let studentClass = "";
let clickCount = 0; 
let coinAwarded = false; 
let quizQuestions = []; 

function shuffleArray(array) {
    for (let i = array.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        const temp = array[i];
        array[i] = array[j];
        array[j] = temp;
    }
}

// ########## AGGRESSIVE Symbol Cleaning Function ##########
function cleanText(text) {
    // 1. Remove all LaTeX/Math environment delimiters and commands ($, \text{}, \frac, \to, {, }, _, ^, \ etc.)
    let cleaned = text.replace(/(\$\$|\\|{|}|_|\$|\*|\\to|\\rightarrow|\\Rightarrow|\\leftarrow)/g, '');
    
    // 2. Handle specific LaTeX commands like \text{...} and \frac{...}{...}
    // This is a simplified aggressive replacement for common math notation to get plain text
    cleaned = cleaned.replace(/text/g, ''); 
    cleaned = cleaned.replace(/ext/g, ''); 
    cleaned = cleaned.replace(/frac/g, '/'); 

    // 3. Remove remaining single backslash commands like \O, \N, etc.
    cleaned = cleaned.replace(/\\([a-zA-Z]+)/g, ''); 
    
    // 4. Remove excessive spaces
    cleaned = cleaned.replace(/(\s\s+)/g, ' '); 
    
    return cleaned.trim();
}


// ########## Screen Control Functions ##########

function updateCoinDisplay() {
    document.querySelector('#current-score span').textContent = totalCoins;
}

function showStudyMaterial() {
    studentName = document.getElementById('student-name').value.trim();
    studentRoll = document.getElementById('student-roll').value.trim();
    studentClass = document.getElementById('student-class').value.trim();

    if (studentName === "" || studentRoll === "" || studentClass === "") {
        alert("कृपया अपना नाम, रोल नंबर और कक्षा भरें।");
        return;
    }
    
    // --- Prepare Study Material Content ---
    const studyListDiv = document.getElementById('study-material-list');
    studyListDiv.innerHTML = '';
    
    // 1. Add Short Answer Questions
    shortAnswerQuestions.forEach((item, index) => {
        const itemDiv = document.createElement('div');
        itemDiv.classList.add('qa-item');
        
        // CLEANING APPLIED: Removing symbols from Q/A text before rendering
        const cleanQuestion = cleanText(item.q);
        const cleanAnswer = cleanText(item.a).replace(/\n/g, '<br>'); // Handle newlines
        
        const questionHtml = `<div class="qa-question">**प्रश्न (विस्तृत) ${index + 1}:** ${cleanQuestion}</div>`;
        const answerHtml = `<div class="qa-answer">**उत्तर:** ${cleanAnswer}</div>`;
        
        itemDiv.innerHTML = questionHtml + answerHtml;
        studyListDiv.appendChild(itemDiv);
    });

    // 2. Add Objective Questions
    const hr = document.createElement('hr');
    studyListDiv.appendChild(hr);
    
    const objHeading = document.createElement('h3');
    objHeading.style.color = '#008080';
    objHeading.textContent = 'वस्तुनिष्ठ प्रश्नोत्तर (क्विज अभ्यास के लिए)';
    studyListDiv.appendChild(objHeading);
    

    objectiveQuestions.forEach((q, index) => {
        const itemDiv = document.createElement('div');
        itemDiv.classList.add('qa-item');
        
        const correctOptionIndex = q.answer;
        
        // CLEANING APPLIED: Removing symbols from Q/A text before rendering
        const cleanQWord = cleanText(q.word);
        const correctOptionText = cleanText(q.options[correctOptionIndex]);
        
        const questionHtml = `<div class="qa-question">**प्रश्न ( वस्तुनिष्ठ) ${index + 1}:** ${cleanQWord}</div>`;
        const answerHtml = `<div class="qa-answer">**सही उत्तर:** ${correctOptionText}</div>`;
        
        itemDiv.innerHTML = questionHtml + answerHtml;
        studyListDiv.appendChild(itemDiv);
    });

    document.getElementById('info-screen').style.display = 'none';
    document.getElementById('study-screen').style.display = 'block';
}


function startQuiz() {
    // Select 20 random questions from the pool of 30
    const pool = [...objectiveQuestions]; 
    shuffleArray(pool); 
    quizQuestions = pool.slice(0, 20); // Using 20 questions

    currentQuestionIndex = 0; 
    totalCoins = 0; 

    document.getElementById('quiz-screen').querySelector('h2').textContent = `📝 रासायनिक अभिक्रियाएँ एवं समीकरण पर आधारित ${quizQuestions.length} वस्तुनिष्ठ प्रश्न`;


    document.getElementById('study-screen').style.display = 'none';
    document.getElementById('quiz-screen').style.display = 'block';
    updateCoinDisplay();
    loadNextQuestion();
}

function finishQuiz() {
    document.getElementById('quiz-screen').style.display = 'none';
    document.getElementById('result-screen').style.display = 'block';
    generateMarkSheet();
}

function handleNextQuestion() {
    currentQuestionIndex++;
    loadNextQuestion();
}

// ########## Quiz Logic ##########

function loadNextQuestion() {
    if (currentQuestionIndex >= quizQuestions.length) {
        finishQuiz();
        return;
    }

    clickCount = 0;
    coinAwarded = false; 
    document.getElementById('next-button').style.display = 'none';
    const quizArea = document.getElementById('quiz-area');
    quizArea.innerHTML = ''; 
    
    const feedbackDiv = document.getElementById('quiz-screen').querySelector('#feedback-message');
    if (feedbackDiv) feedbackDiv.innerHTML = '';

    const qData = quizQuestions[currentQuestionIndex];
    
    // CLEANING APPLIED: Cleaning question word and options before displaying
    const cleanQWord = cleanText(qData.word);

    const html = `
        <div class="question-box">
            <h3>प्रश्न ${currentQuestionIndex + 1}: ${cleanQWord}</h3>
            <div id="feedback-message" style="margin-bottom: 10px;"></div>
            ${qData.options.map((option, index) => `
                <div class="option" data-index="${index}" onclick="handleOptionClick(this, ${index})">${String.fromCharCode(65 + index)}. ${cleanText(option)}</div>
            `).join('')}
        </div>
    `;
    quizArea.innerHTML = html;
}

function handleOptionClick(element, optionIndex) {
    if (document.getElementById('next-button').style.display !== 'none') {
        return; 
    }

    const qData = quizQuestions[currentQuestionIndex];
    const isCorrect = (optionIndex === qData.answer);
    const feedbackElement = document.getElementById('feedback-message');
    
    clickCount++;

    if (isCorrect) {
        element.classList.add('correct');
        
        if (clickCount === 1) {
            totalCoins += 2;
            coinAwarded = true;
            feedbackElement.innerHTML = `<span style="color: #28a745; font-weight: bold;">**उत्कृष्ट!** यह आपका पहला सही प्रयास है। आपको 2 अंक मिले! 🌟</span>`;
        } else if (clickCount > 1 && !coinAwarded) {
            feedbackElement.innerHTML = `<span style="color: #28a745;">यह सही उत्तर है, लेकिन आपको अंक नहीं मिले क्योंकि यह आपका पहला क्लिक नहीं था।</span>`;
        }
        
        document.querySelectorAll('.option').forEach(opt => opt.onclick = null);
        document.getElementById('next-button').style.display = 'block';
        updateCoinDisplay();

    } else {
        element.classList.add('incorrect');
        
        if (clickCount === 1) {
            coinAwarded = false; 
            feedbackElement.innerHTML = `<span style="color: #dc3545; font-weight: bold;">**गलत!** इस प्रश्न के लिए कोई अंक नहीं मिलेगा। ❌</span>`;
            
            // Allow only one more click on the correct answer if not already clicked.
            document.querySelectorAll('.option').forEach(opt => opt.onclick = null);
            document.querySelector(`.option[data-index="${qData.answer}"]`).onclick = function() {
                handleOptionClick(this, qData.answer);
            };
        } else {
            feedbackElement.innerHTML = `<span style="color: #dc3545;">अभी भी गलत है। अब कोई अंक नहीं मिलेगा। ❌</span>`;
            element.onclick = null; 
        }
    }
}

// ########## Generate Mark Sheet ##########

function generateMarkSheet() {
    const totalQuestions = quizQuestions.length; // Will be 20
    const maxCoins = totalQuestions * 2; // Will be 40
    
    const percentage = (totalCoins / maxCoins) * 100;
    
    let grade;
    if (percentage >= 90) grade = "A+ (अति उत्कृष्ट)";
    else if (percentage >= 80) grade = "A (उत्कृष्ट)";
    else if (percentage >= 70) grade = "B+ (बहुत अच्छा)";
    else if (percentage >= 60) grade = "B (अच्छा)";
    else grade = "C (सुधार की आवश्यकता)";

    const markSheetDiv = document.getElementById('mark-sheet');
    markSheetDiv.innerHTML = `
        <p><strong>स्कूल का नाम:</strong> ${SCHOOL_NAME}</p>
        <p><strong>विद्यार्थी का नाम:</strong> ${studentName}</p>
        <p><strong>रोल नंबर:</strong> ${studentRoll}</p>
        <p><strong>कक्षा:</strong> ${studentClass}</p>
        <hr>
        <p><strong>कुल प्रश्न (वस्तुनिष्ठ):</strong> ${totalQuestions}</p>
        <p><strong>अधिकतम अंक:</strong> ${maxCoins}</p>
        <p><strong>प्राप्त कुल अंक:</strong> <strong style="font-size: 24px;">${totalCoins}</strong></p>
        <p><strong>प्रतिशत (अंकों के आधार पर):</strong> <strong>${percentage.toFixed(2)}%</strong></p>
        <p><strong>ग्रेड:</strong> <strong style="font-size: 28px; color: ${percentage >= 80 ? '#28a745' : '#ffc107'};">${grade}</strong></p>
    `;
}